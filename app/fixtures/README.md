# fixtures — 정적 데모 데이터

이 디렉터리의 값은 **실제 API 응답이 아니라 프로토타입용 정적 예시**입니다.

- Claude / Finnhub 등 실제 API 와 연결돼 있지 않습니다.
- 실데이터로 오해되지 않도록 제품 코드/네트워크 계층과 분리해 둡니다.
- 실제 연동은 다음 단계에서 `spikes/` 의 검증된 어댑터를 재사용합니다.
- 화면에도 "데모용 예시 데이터" 문구를 함께 노출합니다.
